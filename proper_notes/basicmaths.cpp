// gcd (a,b) = gcd(a-b,b)
// the above formula only works when a > b

// apart from this we can also write this formula
// gcd(a,b)  = gcd(a%b) till one of them becomes 0 and if one become 0 than the other is gcd